# rustic-boot

Application bootstrap utilities for the rustic-ai workspace.

## Status

Placeholder — this crate is reserved for shared startup logic (config loading, routing helpers, logger initialisation) that will be extracted from external dependencies as the workspace matures.

The API binary currently uses `agentic-boot` for these concerns; this crate will replace it over time.
